---
:backends:
  - eyaml
  - yaml

:hierarchy:
  - "%{::environment}/%{calling_module}"
  - shared_secret
  - common

:yaml:
  :datadir: '/etc/puppet/hiera/data'

:gpgpghome: '/etc/puppet/hiera/gpg'

:eyaml:
  :datadir: '/etc/puppet/hiera/data'
  :gpg_gnupghome: '/etc/puppet/hiera/gpg'

